name: Build and Publish Dashboard Images

on:
  push:
    branches: [ "main" ]

env:
  REGISTRY: ghcr.io
  OWNER: ${{ github.repository_owner }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push — backend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: bet_dashboard/backend/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ env.REGISTRY }}/${{ env.OWNER }}/bet-assistant-backend:latest
          provenance: false
          cache-from: type=gha,scope=backend
          cache-to:   type=gha,mode=max,scope=backend

      - name: Build and push — frontend
        uses: docker/build-push-action@v5
        with:
          context: bet_dashboard/frontend
          file: bet_dashboard/frontend/Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ env.REGISTRY }}/${{ env.OWNER }}/bet-assistant-frontend:latest
          provenance: false
          cache-from: type=gha,scope=frontend
          cache-to:   type=gha,mode=max,scope=frontend

      - name: Build and push — runner
        uses: docker/build-push-action@v5
        with:
          context: .
          file: setup/runner.Dockerfile
          push: true
          platforms: linux/amd64
          tags: ${{ env.REGISTRY }}/${{ env.OWNER }}/bet-assistant-runner:latest
          provenance: false
          cache-from: type=gha,scope=runner
          cache-to:   type=gha,mode=max,scope=runner
